<div class="marquee-wrapper">
  <div class="marquee-content">
    <span>PREMIUM DESIGN • NEXT GEN SHOPIFY • PLANEGOSA STUDIO • SPEED OPTIMIZED • </span>
    <span>PREMIUM DESIGN • NEXT GEN SHOPIFY • PLANEGOSA STUDIO • SPEED OPTIMIZED • </span>
  </div>
</div>

<style>
  .marquee-wrapper {
    background: #fff;
    color: #000;
    padding: 20px 0;
    overflow: hidden;
    white-space: nowrap;
    display: flex;
    font-weight: 900;
    font-size: 1.5rem;
    transform: rotate(-2deg) scale(1.1);
    margin: 50px 0;
  }
  .marquee-content {
    display: inline-block;
    animation: marquee 20s linear infinite;
  }
  @keyframes marquee {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }
  .marquee-content span { padding-right: 50px; }
</style>

{% schema %}
{ "name": "Marquee Text", "presets": [{ "name": "Marquee Text" }] }
{% endschema %}
